name = "wsdeck-ai-proxy"
main = "src/index.ts"
compatibility_date = "2025-01-01"

# Cloudflare Workers AI 綁定，讓 src/index.ts 可以用 env.AI.run(...)
# 呼叫內建模型，不用另外辦外部服務的 API Key
[ai]
binding = "AI"

# APP_SHARED_SECRET 不寫在這裡（會進 git），用 `wrangler secret put` 設定，見 README。
